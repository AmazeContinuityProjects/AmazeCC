import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amazecontinuityprojects.amazecc',
  appName: 'AmazeCC',
  webDir: 'out'
};

export default config;
